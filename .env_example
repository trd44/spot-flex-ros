# Spot robot connection settings
# Create a .env file with the actual credentials
# I didn't put the real .env file in the repo because it has our username and password

SPOT_IP=192.168.80.3
SPOT_USERNAME=spot_username
SPOT_PASSWORD=spot_password
